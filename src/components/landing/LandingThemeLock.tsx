'use client';

export function LandingThemeLock() {
  // Intentionally no-op: landing styling is scoped by `.theme-landing`.
  // Do not mutate global theme state from this component.
  return null;
}
